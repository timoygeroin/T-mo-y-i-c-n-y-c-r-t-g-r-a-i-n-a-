services:
  organism:
    image: node:24
    working_dir: /app/organism
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - MONDAY_DB=/app/organism/var/monday.sqlite
    command: sh -c "mkdir -p var && node src/server.js"
    ports:
      - "3000:3000"
