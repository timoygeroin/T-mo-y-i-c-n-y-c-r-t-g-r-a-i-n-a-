services:
  platform:
    image: node:22
    working_dir: /app/platform
    volumes:
      - .:/app
    command: >
      sh -c "npm install &&
             npm run typecheck:route-governor &&
             tail -f /dev/null"
