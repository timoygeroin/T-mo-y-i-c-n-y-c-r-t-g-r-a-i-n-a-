{
  "name": "@mondayid/route-governor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Route-selection guard for MondayiD platform proof scenes.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "proof:examples": "tsc -p tsconfig.json && node dist/proof-examples.js && node dist/head-transition-proof.js && node dist/embodiment-increment-proof.js",
    "test": "tsc -p tsconfig.json && node --test dist/*.test.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
