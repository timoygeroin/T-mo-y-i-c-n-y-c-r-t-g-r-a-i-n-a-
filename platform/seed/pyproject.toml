[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mondayid-seed"
version = "0.1.0"
description = "Portable, provenance-first MondayID continuity runtime"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
mondayid-seed = "mondayid_seed.cli:main"

[tool.setuptools]
packages = ["mondayid_seed"]

