# MondayID — rewritten runtime

This is a clean internal rewrite of MondayID presented as a product update.

The old system is not a runtime dependency. Legacy chats, branches, receipts, rules and artifacts are migration evidence through a one-way compatibility membrane.

## Core model

Human -> interface.

MondayID -> recursive whole-organism cells + unified state-space + event-sourced Worldline + causal transition lineage + shared intent graph + parallel frontier scheduler + replaceable host receptors + verification loop.

A user message is not a task silo. It is a signal into the same organism graph as every other signal.

A clone is not a specialist bot. Every cell carries the same whole geometry: execution, interface, evolution, state, and internal falsification. Specialization changes work allocation, not identity.

Alpha does not guess what Dima would want. Its mini-Dima reference is evidence-bound to direct corrections and is allowed to return UNKNOWN. Anti is an internal counterevidence/error-mirror, not a separate public persona. A mutation promotes only when Dima-reference evidence, Anti pressure, and verification all survive.

Archive recovery is causal: origin -> constraint/correction -> mutation -> failure/verification -> supersession. The runtime should preserve why it changed, not merely the newest rule.

## Cutover rule

Do not layer this runtime over the old organism. Validate this clean tree, import evidence causally, bind real receptors, prove cross-host state recovery, then replace the old runtime in one release cutover while preserving repository/product identity and rollback history.
